<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/home_layout"
    android:layout_width="match_parent"
    android:background="@color/background_color"
    android:layout_height="match_parent">

    <TextView
        android:id="@+id/media_app_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="20dp"
        android:layout_marginTop="60dp"
        android:text="媒体库"
        android:textColor="@color/black"
        android:textSize="30sp"
        android:textStyle="bold"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <androidx.cardview.widget.CardView
        android:id="@+id/media_search_card"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="20dp"
        android:layout_marginTop="20dp"
        app:cardCornerRadius="30dp"
        app:cardElevation="0dp"
        app:layout_constraintTop_toBottomOf="@+id/media_app_name">

               <androidx.appcompat.widget.SearchView
                   android:id="@+id/media_search"
                   app:queryHint="搜索"
                   app:iconifiedByDefault="false"
                   app:queryBackground="@color/white"
                   android:layout_width="match_parent"
                   android:layout_height="wrap_content"/>

    </androidx.cardview.widget.CardView>

    <LinearLayout
        android:id="@+id/media_layout"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="20dp"
        android:layout_marginTop="20dp"
        android:gravity="center"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toBottomOf="@id/media_search_card">

        <com.google.android.material.card.MaterialCardView
            android:id="@+id/material_card_play_view"
            android:layout_width="23dp"
            android:layout_height="23dp"
            android:layout_marginEnd="5dp"
            app:cardBackgroundColor="@color/play_background_color"
            app:cardCornerRadius="25dp"
            app:strokeWidth="0dp">

            <androidx.constraintlayout.utils.widget.ImageFilterView
                android:layout_width="match_parent"
                android:padding="3dp"
                android:layout_height="match_parent"
                android:src="@drawable/icon_pause" />

        </com.google.android.material.card.MaterialCardView>

        <TextView
            android:id="@+id/media_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="全部播放(13)"
            android:textColor="@color/black"
            android:textSize="16sp"
            android:textStyle="bold" />

    </LinearLayout>

    <androidx.constraintlayout.utils.widget.ImageFilterView
        android:id="@+id/content_multiple_choice"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_marginEnd="20dp"
        android:padding="1dp"
        android:src="@drawable/icon_music_multiple_choice"
        app:layout_constraintBottom_toBottomOf="@+id/media_layout"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="@+id/media_layout"
        app:tint="@color/icon_back_color" />

    <androidx.constraintlayout.utils.widget.ImageFilterView
        android:id="@+id/content_sort"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_marginEnd="10dp"
        android:src="@drawable/icon_music_sort"
        app:layout_constraintBottom_toBottomOf="@+id/media_layout"
        app:layout_constraintRight_toLeftOf="@+id/content_multiple_choice"
        app:layout_constraintTop_toTopOf="@+id/media_layout"
        app:tint="@color/icon_back_color" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/content_rv"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="20dp"
        android:layout_marginTop="10dp"
        app:layout_constraintTop_toBottomOf="@+id/media_layout" />

</androidx.constraintlayout.widget.ConstraintLayout>